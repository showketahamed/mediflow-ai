# Use the Supavisor session pooler on port 5432 for the persistent Railway API
# and Prisma migrations. URL-encode special characters in the password.
DATABASE_URL=postgresql://prisma.PROJECT_REF:SUPABASE_DATABASE_PASSWORD@REGION.pooler.supabase.com:5432/postgres?sslmode=require
